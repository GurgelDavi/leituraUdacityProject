import {combineReducers} from 'redux'
import {

} from '../actions'


function reducer(state = {}, action) {
  switch(action.type){
    default :
      return state
  }
}
export default combineReducers({
  reducer,
})
